spring:
  profiles: production
  data:
    mongodb:
      uri: mongodb://localhost:27017/erbaldb-users
      repositories:
        enabled: true
server:
  port: 8080
  context-path: /erbal

---
spring:
  profiles: testing
  data:
    mongodb:
      uri: mongodb://localhost:27017/erbaldb-test
      repositories:
        enabled: true
server:
  port: 9090
  context-path: /erbal-test