server:
  port: 9002
  context-path: /erbal-greenhouse-management

eureka:
  client:
    service-url:
      defaultZone: http://borgo.ddns.net:8761/eureka/

spring:
  data:
    mongodb:
      host: localhost
      username: admin
      password: password
      database: erbal_db
      port: 27017